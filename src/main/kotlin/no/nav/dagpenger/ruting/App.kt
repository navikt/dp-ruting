package no.nav.dagpenger.ruting

fun main() {
    ApplicationBuilder(Configuration.config).start()
    // no.nav.dagpenger.ruting.ApplicationBuilder(Configuration.config).start()
    println("Hello, World!")
}
